# Docker Environment Overrides
# This file extends .env and only overrides values needed for Docker containers

# Production environment for Docker
NODE_ENV=production

# Docker DNS hostnames (override localhost values from .env)
DATABASE_HOST=strapi-db
MONGODB_HOST=backend-db

# Internal Docker service URLs
STRAPI_URL=http://strapi:1337
MONGODB_URI=mongodb://backend_user:backend_password_123@backend-db:27017/educado_backend